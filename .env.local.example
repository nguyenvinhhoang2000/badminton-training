# Supabase — lấy trong Dashboard → Project Settings → API
# Sao chép file này thành .env.local rồi điền giá trị thật.
# Không có 2 biến này thì app vẫn chạy bình thường ở chế độ offline (chỉ localStorage).
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-public-key...
